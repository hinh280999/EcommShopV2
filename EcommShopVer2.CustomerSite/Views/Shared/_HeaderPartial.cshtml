<header >
    <div class="">
        <nav class="navbar navbar-light bg-light d-flex justify-content-around">
            <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index">eCommShop.CustomerSite</a>
            <form class="form-inline">
                <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
                <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
            </form>
            <partial name="_LoginPartial" />
        </nav>
    </div>
    <div class="sticky-top">
        <nav class="navbar  navbar-expand-lg navbar-dark bg-dark" style="box-shadow : 0 1px 10px #999;">
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item">
                        <a class="nav-link text-white" asp-area="" asp-controller="Home" asp-action="Index">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white" asp-area="" asp-controller="Home" asp-action="Products">Products</a>
                    </li>
                    @*@await Component.InvokeAsync("CategoriesNavbar")
                    <li class="nav-item">
                        @if (HttpContextAccessor.HttpContext.Session.Get<List<eCartItem>>("SessionCart") != null)
                        {
                            var count = HttpContextAccessor.HttpContext.Session.Get<List<eCartItem>>("SessionCart").Count;
                            <a class="nav-link text-white" asp-controller="Cart" asp-action="Index"><i class="fas fa-shopping-cart"></i>&nbsp;Cart<span id="CartItemNumbs" class="badge bg-secondary">@count</span></a>
                        }
                        else
                        {
                            <a class="nav-link text-white" asp-area="" asp-controller="Cart" asp-action=""><i class="fas fa-shopping-cart"></i>&nbsp;Cart<span id="CartItemNumbs" class="badge bg-secondary"></span></a>
                        }
                    </li>*@
                    @if (User.Identity.IsAuthenticated)
                    {
                        <li class="nav-item">
                            <a class="nav-link text-white" asp-area="" asp-controller="Order" asp-action="GetAllOrders">Orders</a>
                        </li>
                    }
                </ul>
            </div>
        </nav>
    </div>
    
</header>